const App = () => (
  <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
    <h1>Email Tool Frontend</h1>
    <p>This is a minimal React app.</p>
  </div>
);

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
